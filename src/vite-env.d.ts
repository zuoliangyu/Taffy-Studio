/// <reference types="vite/client" />

// Injected by Vite at build time (see vite.config.ts `define`). True when the
// frontend is built under the Tauri CLI, false for a plain web build.
declare const __IS_TAURI__: boolean
