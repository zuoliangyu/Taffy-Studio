# Use the China mirror for npm packages. Cuts install time from minutes to seconds
# on mainland networks; also speeds up the Linux/Android Dockerfile builds because
# they copy this file in and inherit the registry override.
#
# If you ever need to bypass: pnpm install --registry=https://registry.npmjs.org
# Or delete this file entirely.
registry=https://registry.npmmirror.com
